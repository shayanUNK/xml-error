import Swal from "sweetalert2";

let submit = document.querySelector("#error200");
submit.addEventListener("click", submitOutput);

function submitOutput() {
  const xhr = new XMLHttpRequest();

  xhr.open("GET", "");
}
